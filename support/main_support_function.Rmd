# Write and Apply Function

dat.files  <- list.files(path="Main Dir",
                                recursive=T,
                                pattern="RMSD.dat"
                                ,full.names=T)
